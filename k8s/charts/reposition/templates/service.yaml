apiVersion: v1
kind: Service
metadata:
  name: {{ include "reposition.fullname" . }}
  labels:
    {{- include "reposition.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reposition.selectorLabels" . | nindent 4 }}
